Place the legacy initialization file and, if needed, the acquisition background
image here or update the paths in `settings.toml`.

No synthetic SRN2 solution is included because the supplied material contained
only the Python source, not the numerical `.init` state or patron image.
